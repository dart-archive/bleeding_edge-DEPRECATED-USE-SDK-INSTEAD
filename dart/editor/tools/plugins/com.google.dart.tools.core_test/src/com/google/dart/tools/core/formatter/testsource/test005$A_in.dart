class Test {
  static final template = """
Start${
    start + end
    /* comment 1
    comment 2 */
  }End
""";
}

/* --- *
 * S t r e n g t h
 * --- */

//error with \b) ?
//That's all, folks. Copyright (c) 2011, the Dart project authors.
//All Rights Reserved.
 class Havlak {
  /** That's it, then.
   */
  void x(int header) {
  // Path Compression, all nodes' parents point to the 1st level parent.
      header[0] = 0;
        // Step c:
        //
        // The outer loop, unchanged from Tarjan. It does nothing except
        // for those nodes which are the destinations of backedges.
        // For a header node w, we chase backward from the sources of the
        // backedges adding nodes to the set P, representing the body of
        // the loop headed by w.
        //
        // By running through the nodes in reverse of the DFST preorder,
        // we ensure that inner loop headers will be processed before the
        // headers for surrounding loops.
    edgeList = new QueueImplementation<BasicBlockEdge>(); // error if
                                                          // < is (
    int a;
    }
  Havlak() : cfg=new CFG(), lsg=new LSG() {
    root = cfg.createNode(0);
  }

/**-
 * Class LoopStructureGraph
 *
 * Loop Structure Graph - Scaffold Code
 *
 * Maintain loop structure for a given CFG.
 *
 * Two values are maintained for this loop graph, depth, and nesting level.
 * For example:
 *
 * loop        nesting level    depth
 *----------------------------------------
 * loop-0      2                0
 *   loop-1    1                1
 *   loop-3    1                1
 *     loop-2  0                2
 */
  void dump() {
    basicBlockMap.forEach(int f(int n, BasicBlock bb) {
      bb.dump();
    });
  }
}
